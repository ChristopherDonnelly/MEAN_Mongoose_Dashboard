<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Mongoose Dashboard (for the birds)</title>
    <link rel='stylesheet' href='/css/style.css' />
</head>
<body>
    <div id="wrapper">
        <div id="nav">
            <a href="/">Home</a>
            <span><em>New Bird</em></span>
        </div>
        <h1>Mongoose Dashboard (for the birds)</h1> 
        <div id="content_layer">
            <form action="/birds" id="add_bird" method="post">
                <label for="name">Bird Name: 
                    <input type="text" name="name" id="name" placeholder="Enter Name"></label>
                <label for="breed">Bird Breed: 
                    <input type="text"name="breed" id="breed" placeholder="Enter Breed"></label>
                <span>
                    <input type="submit" name="submit" value="Add New Bird">
                </span>
            </form>
            <div id="errors">
                <% if(typeof(errors) != 'undefined' ) { %>
                    <% for (var x in errors) { %>
                        <h3><%= errors[x].message %></h3>
                    <% } %>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>